import Foundation

// MARK: - Contacts Constants

/// Constants for the ContactsCRUD feature (Activity 27).
enum ContactsConstants {
    /// Number of digits in a Brazilian postal code (CEP).
    static let cepDigitCount: Int = 8
    /// System image used in the contacts list empty state.
    static let emptyStateIcon: String = "person.2"
    /// System image used in the toolbar add button.
    static let addIcon: String = "plus"
    /// System image used in the toolbar info button.
    static let infoIcon: String = "info.circle"
}
