> **Platform**: Android

# ⏳ ZodiakSkeletonLine / ZodiakSkeletonRect / ZodiakSkeletonCircle

iOS: Loading placeholder shapes.

**Android placeholder:**
```kotlin
// TODO: replace with ZodiakSkeleton* when ported
// Use Modifier.shimmer() from a shimmer library, or:
Box(
    modifier = Modifier
        .fillMaxWidth()
        .height(16.dp)
        .background(
            color = MaterialTheme.colorScheme.surfaceVariant,
            shape = MaterialTheme.shapes.extraSmall,
        )
)
```
