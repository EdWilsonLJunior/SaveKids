> **Platform**: Android

# ⏳ ZodiakNotificationBanner — App Notifications

iOS: App notification with optional CTA, variants: `.information` / `.positive` / `.warning`

**Android placeholder:**
```kotlin
// TODO: replace with ZodiakNotificationBanner when ported
Snackbar(
    action = { TextButton(onClick = onAction) { Text(actionLabel) } }
) { Text(message) }
```

---
