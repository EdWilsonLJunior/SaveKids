> **Platform**: Android

# ⏳ ZodiakBottomSheet — Sheet Drawer

iOS: Sheet drawer from bottom, lighter than a modal.

**Android placeholder:**
```kotlin
// TODO: replace with ZodiakBottomSheet when ported
if (uiState.showSheet) {
    ModalBottomSheet(onDismissRequest = viewModel::dismissSheet) {
        // content
    }
}
```

---
