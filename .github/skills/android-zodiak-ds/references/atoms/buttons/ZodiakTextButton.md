> **Platform**: Android

# ZodiakTextButton — Low priority ✅ Ported

```kotlin
@Composable
fun ZodiakTextButton(
    text: String,
    onClick: () -> Unit,
    modifier: Modifier = Modifier,
    enabled: Boolean = true,
)
```

**When to use:** Lowest-priority action — text only, no background. Links, auxiliary actions. No limit per screen.

---
