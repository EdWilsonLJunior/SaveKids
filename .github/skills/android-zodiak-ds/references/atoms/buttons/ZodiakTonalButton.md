> **Platform**: Android

# ZodiakTonalButton — Mid-priority ✅ Ported

```kotlin
@Composable
fun ZodiakTonalButton(
    text: String,
    onClick: () -> Unit,
    modifier: Modifier = Modifier,
    enabled: Boolean = true,
)
```

**When to use:** Secondary action with visual weight between filled and outlined. Use when two buttons of similar importance are shown together.

---
